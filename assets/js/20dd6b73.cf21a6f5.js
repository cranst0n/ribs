"use strict";(globalThis.webpackChunkwebsite=globalThis.webpackChunkwebsite||[]).push([[8239],{5532(e,n,r){r.r(n),r.d(n,{assets:()=>d,contentTitle:()=>l,default:()=>p,frontMatter:()=>c,metadata:()=>i,toc:()=>u});const i=JSON.parse('{"id":"effect/overview","title":"Overview","description":"ribs_effect is a functional effects library for Dart, ported from the Scala","source":"@site/docs/effect/overview.mdx","sourceDirName":"effect","slug":"/effect/overview","permalink":"/ribs/docs/effect/overview","draft":false,"unlisted":false,"editUrl":"https://github.com/cranst0n/ribs/edit/main/website/docs/effect/overview.mdx","tags":[],"version":"current","sidebarPosition":0,"frontMatter":{"sidebar_position":0},"sidebar":"tutorialSidebar","previous":{"title":"Syntax","permalink":"/ribs/docs/core/syntax"},"next":{"title":"IO","permalink":"/ribs/docs/effect/io"}}');var t=r(4848),a=r(8453),o=r(2045),s=r(8421);const c={sidebar_position:0},l="Overview",d={},u=[{value:"The core idea: effects as values",id:"the-core-idea-effects-as-values",level:2},{value:"What the package provides",id:"what-the-package-provides",level:2},{value:"IO",id:"io",level:3},{value:"Resource",id:"resource",level:3},{value:"Ref",id:"ref",level:3},{value:"Deferred",id:"deferred",level:3},{value:"Queue",id:"queue",level:3},{value:"Semaphore",id:"semaphore",level:3},{value:"IO Retry",id:"io-retry",level:3},{value:"Concurrency model",id:"concurrency-model",level:2},{value:"Execution",id:"execution",level:2}];function h(e){const n={a:"a",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",p:"p",pre:"pre",...(0,a.R)(),...e.components};return(0,t.jsxs)(t.Fragment,{children:[(0,t.jsx)(n.header,{children:(0,t.jsx)(n.h1,{id:"overview",children:"Overview"})}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"ribs_effect"})," is a functional effects library for Dart, ported from the Scala\n",(0,t.jsx)(n.a,{href:"https://typelevel.org/cats-effect/",children:"Cats Effect"})," library. It gives you a\nprincipled toolkit for writing programs that interact with the world \u2014\nperforming I/O, managing resources, sharing mutable state, and running\nconcurrent tasks \u2014 all without sacrificing predictability or testability."]}),"\n",(0,t.jsx)(n.h2,{id:"the-core-idea-effects-as-values",children:"The core idea: effects as values"}),"\n",(0,t.jsxs)(n.p,{children:["Dart's built-in ",(0,t.jsx)(n.code,{children:"Future"})," executes immediately when created. This eager\nevaluation makes a ",(0,t.jsx)(n.code,{children:"Future"})," a ",(0,t.jsx)(n.em,{children:"running computation"}),", not a ",(0,t.jsx)(n.em,{children:"description"})," of\none. That distinction matters: you cannot safely reuse a ",(0,t.jsx)(n.code,{children:"Future"}),", pass it\naround as a value, or compose it with other effects without reasoning carefully\nabout when and how many times it has already been executed."]}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"IO<A>"})," is a lazy, referentially transparent description of an effect. Creating\nan ",(0,t.jsx)(n.code,{children:"IO"})," does nothing on its own \u2014 it is a ",(0,t.jsx)(n.em,{children:"blueprint"}),". The same ",(0,t.jsx)(n.code,{children:"IO"})," value can\nbe reused, composed, and executed any number of times without surprising\nbehaviour:"]}),"\n",(0,t.jsx)(o.z,{language:"dart",title:"IO.delay is lazy \u2014 safe to reuse",snippet:s.A,section:"io-3"}),"\n",(0,t.jsxs)(n.p,{children:["Nothing in the snippet above runs until ",(0,t.jsx)(n.code,{children:".unsafeRunFuture()"})," is called. The\nsame ",(0,t.jsx)(n.code,{children:"rng"})," IO value is used twice, and each use produces an independent result."]}),"\n",(0,t.jsx)(n.h2,{id:"what-the-package-provides",children:"What the package provides"}),"\n",(0,t.jsx)(n.h3,{id:"io",children:"IO"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"IO<A>"})," is the fundamental building block. It models any computation that may\nproduce a value of type ",(0,t.jsx)(n.code,{children:"A"}),", perform side effects, raise errors, or never complete."]}),"\n",(0,t.jsx)(n.h3,{id:"resource",children:"Resource"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Resource<A>"})," models the ",(0,t.jsx)(n.em,{children:"acquire-use-release"})," lifecycle of a value that needs\ncleanup. Finalizers are guaranteed to run regardless of whether the inner\ncomputation succeeds, fails, or is cancelled. Nested resources finalize in\nLIFO order."]}),"\n",(0,t.jsx)(n.h3,{id:"ref",children:"Ref"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Ref<A>"})," is a concurrent, purely functional mutable variable. All updates are\natomic and exposed as ",(0,t.jsx)(n.code,{children:"IO"})," operations so they compose safely with the rest of\nthe effect stack."]}),"\n",(0,t.jsx)(n.h3,{id:"deferred",children:"Deferred"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Deferred<A>"})," is a single-assignment, purely functional promise. A fiber that\ncalls ",(0,t.jsx)(n.code,{children:".value()"})," on an empty ",(0,t.jsx)(n.code,{children:"Deferred"})," will semantically block until another\nfiber calls ",(0,t.jsx)(n.code,{children:".complete(a)"}),". It is the canonical primitive for coordinating two\nindependent fibers."]}),"\n",(0,t.jsx)(n.h3,{id:"queue",children:"Queue"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Queue<A>"})," is a concurrent, back-pressured queue. Multiple producers and\nconsumers can interact with the same queue safely via ",(0,t.jsx)(n.code,{children:"IO"}),". Both bounded and\nunbounded variants are available."]}),"\n",(0,t.jsx)(n.h3,{id:"semaphore",children:"Semaphore"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"Semaphore"})," controls access to a shared resource by limiting the number of\nfibers that can hold a permit simultaneously. It is the standard primitive for\nrate-limiting, connection pooling, and mutual exclusion."]}),"\n",(0,t.jsx)(n.h3,{id:"io-retry",children:"IO Retry"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"ribs_effect"})," includes a retry combinator built on top of ",(0,t.jsx)(n.code,{children:"IO"}),". Policies are\ncomposable values \u2014 cap the number of attempts, add exponential backoff, or\ncombine policies with ",(0,t.jsx)(n.code,{children:"and"}),"/",(0,t.jsx)(n.code,{children:"or"}),"."]}),"\n",(0,t.jsx)(n.h2,{id:"concurrency-model",children:"Concurrency model"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"ribs_effect"})," implements a ",(0,t.jsx)(n.em,{children:"green-thread"})," concurrency model on top of Dart's\nevent loop. A ",(0,t.jsx)(n.code,{children:"Fiber"})," is a lightweight, cancellable unit of concurrency \u2014 far\ncheaper than a Dart ",(0,t.jsx)(n.code,{children:"Isolate"})," (~270 bytes for a created fiber). Calling ",(0,t.jsx)(n.code,{children:".start()"}),"\non any ",(0,t.jsx)(n.code,{children:"IO"})," forks it onto a new fiber, and fibers can be joined, cancelled, or\nraced with ",(0,t.jsx)(n.code,{children:"IO.race"})," and ",(0,t.jsx)(n.code,{children:"IO.both"}),"."]}),"\n",(0,t.jsxs)(n.p,{children:["Cancellation is ",(0,t.jsx)(n.em,{children:"cooperative and safe"}),": when a fiber is cancelled, any\n",(0,t.jsx)(n.code,{children:"Resource"})," finalizers and ",(0,t.jsx)(n.code,{children:"IO.onCancel"})," handlers it holds are guaranteed to run."]}),"\n",(0,t.jsx)(n.h2,{id:"execution",children:"Execution"}),"\n",(0,t.jsxs)(n.p,{children:[(0,t.jsx)(n.code,{children:"IO"})," values are pure descriptions \u2014 they do nothing until explicitly executed.\nAt the edge of your application, call ",(0,t.jsx)(n.code,{children:"unsafeRunFuture()"})," (or\n",(0,t.jsx)(n.code,{children:"unsafeRunFutureOutcome()"})," to inspect the ",(0,t.jsx)(n.code,{children:"Outcome"}),") to hand the computation\nto the Dart runtime:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-dart",children:"void main() {\n  myProgram().unsafeRunFuture();\n}\n"})}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"unsafe"})," prefix is a deliberate signal: this is the boundary where pure\nfunctional code meets the imperative world. Keep this call as close to ",(0,t.jsx)(n.code,{children:"main"}),"\nas possible, and let ",(0,t.jsx)(n.code,{children:"IO"})," handle everything inside."]})]})}function p(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(h,{...e})}):h(e)}},8421(e,n,r){r.d(n,{A:()=>i});const i="// ignore_for_file: avoid_print, unused_local_variable, unused_element\n\nimport 'dart:io';\nimport 'dart:math';\n\nimport 'package:async/async.dart';\nimport 'package:ribs_core/ribs_core.dart';\nimport 'package:ribs_effect/ribs_effect.dart';\n\nFuture<void> snippet1() async {\n  // io-1\n\n  final rng = Future(() => Random.secure().nextInt(1000));\n\n  await rng.then((x) => rng.then((y) => print('x: $x / y: $y')));\n\n  // io-1\n}\n\nFuture<void> snippet2() async {\n  // io-2\n\n  // Substitute the definition of fut with it's expression\n  // x and y are different! (probably)\n  await Future(\n    () => Random.secure().nextInt(1000),\n  ).then((x) => Future(() => Random.secure().nextInt(1000)).then((y) => print('x: $x / y: $y')));\n\n  // io-2\n}\n\nFuture<void> snippet3() async {\n  // io-3\n\n  final rng = IO.delay(() => Random.secure().nextInt(1000));\n\n  // x and y are different! (probably)\n  await rng.flatMap((x) => rng.flatMap((y) => IO.print('x: $x / y: $y'))).unsafeRunFuture();\n\n  // io-3\n}\n\nFuture<void> asyncSnippet1() async {\n  // io-async-1\n\n  IO<A> futureToIO<A>(Function0<Future<A>> fut) {\n    IO.async_<A>((cb) {\n      fut().then(\n        (a) => cb(Right(a)),\n        onError: (Object err, StackTrace st) => cb(Left(err)),\n      );\n    });\n\n    throw UnimplementedError();\n  }\n\n  // io-async-1\n}\n\nFuture<void> errorHandlingSnippet1() async {\n  // error-handling-1\n\n  // composable handler using handleError\n  final ioA = IO.delay(() => 90 / 0).handleError((ex) => 0);\n  final ioB = IO.delay(() => 90 / 0).handleErrorWith((ex) => IO.pure(double.infinity));\n\n  IO<double> safeDiv(int a, int b) => IO.defer(() {\n    if (b != 0) {\n      return IO.pure(a / b);\n    } else {\n      return IO.raiseError('cannot divide by 0!');\n    }\n  });\n\n  // error-handling-1\n}\n\nvoid safeResourcesSnippet1() {\n  // safe-resources-1\n  final sink = File('path/to/file').openWrite();\n\n  try {\n    // use sink...\n  } catch (e) {\n    // catch error\n  } finally {\n    sink.close();\n  }\n\n  // safe-resources-1\n}\n\nvoid safeResourcesSnippet2() {\n  // safe-resources-2\n\n  final sink = IO.delay(() => File('path/to/file')).map((f) => f.openWrite());\n\n  // bracket *ensures* that the sink is closed\n  final program = sink.bracket(\n    (sink) => IO.exec(() => sink.writeAll(['Hello', 'World'])),\n    (sink) => IO.exec(() => sink.close()),\n  );\n\n  // safe-resources-2\n}\n\nFuture<void> conversionsSnippet() async {\n  // conversions-1\n\n  IO.fromOption(const Some(42), () => Exception('raiseError: none'));\n\n  IO.fromEither(Either.right(42));\n\n  IO.fromFuture(IO.delay(() => Future(() => 42)));\n\n  IO.fromCancelableOperation(\n    IO.delay(\n      () => CancelableOperation.fromFuture(\n        Future(() => 32),\n        onCancel: () => print('canceled!'),\n      ),\n    ),\n  );\n\n  // conversions-1\n\n  // conversions-bad-future\n\n  final fut = Future(() => print('bad'));\n\n  // Too late! Future is already running!\n  final ioBad = IO.fromFuture(IO.pure(fut));\n\n  // IO.pure parameter is not lazy so it's evaluated immediately!\n  final ioAlsoBad = IO.fromFuture(IO.pure(Future(() => print('also bad'))));\n\n  // Here we preserve laziness so that ioGood is referentially transparent\n  final ioGood = IO.fromFuture(IO.delay(() => Future(() => print('good'))));\n\n  // conversions-bad-future\n}\n\nvoid deferSnippet() {\n  // io-defer\n\n  // IO.delay: synchronous thunk that produces a plain value A\n  final delayEx = IO.delay(() => Random.secure().nextInt(100));\n\n  // IO.defer: thunk that produces an IO<A> \u2014 use when choosing between IOs\n  // at runtime, or when IO construction itself could throw\n  IO<int> coinFlip() => IO.defer(() {\n    final heads = Random.secure().nextBool();\n    return heads ? IO.pure(1) : IO.raiseError('tails!');\n  });\n\n  // io-defer\n}\n\nvoid combinatorsSnippet() {\n  // io-combinators\n\n  // map: transform the successful value\n  final doubled = IO.pure(21).map((n) => n * 2); // IO(42)\n\n  // flatMap: sequence two IOs, passing the result of the first to the second\n  final chained = IO.pure(10).flatMap((n) => IO.pure(n + 5)); // IO(15)\n\n  // flatTap: run a side-effect using the value, then pass the value through\n  final logged = IO.pure(42).flatTap((n) => IO.print('computed: $n')); // IO(42)\n\n  // productR: run two IOs in sequence, keep only the second result\n  final init = IO.print('setup').productR(IO.pure(42)); // IO(42)\n\n  // attempt: pull a potential error into an Either instead of raising it\n  final safe = IO.raiseError<int>('oops').attempt(); // IO<Either<Object, int>>\n\n  // redeem: recover from an error or transform the success, all in one step\n  final recovered = IO.raiseError<int>('oops').redeem((_) => -1, (n) => n * 2); // IO(-1)\n\n  // io-combinators\n}\n\nvoid concurrencySnippet() {\n  // io-both\n\n  final fetchUser = IO.pure('Alice');\n  final fetchProfile = IO.pure(99);\n\n  // Both IOs run concurrently; if either fails the other is canceled\n  final program = IO\n      .both(fetchUser, fetchProfile)\n      .flatMap((t) => IO.print('user: ${t.$1}, score: ${t.$2}'));\n\n  // io-both\n\n  // io-race\n\n  final fast = IO.sleep(100.milliseconds).productR(IO.pure('fast result'));\n  final slow = IO.sleep(500.milliseconds).productR(IO.pure('slow result'));\n\n  // The winner is returned as Either<A, B>; the loser is automatically canceled\n  final race = IO\n      .race(fast, slow)\n      .map((winner) => winner.fold((a) => 'A won: $a', (b) => 'B won: $b'));\n\n  // io-race\n}\n\nvoid timingSnippet() {\n  // io-timing\n\n  // sleep: deliberate async delay\n  final delayed = IO.sleep(200.milliseconds).productR(IO.pure(42));\n\n  // timed: measure how long an IO takes\n  final measured = IO.pure(42).timed(); // IO<(Duration, int)>\n\n  // timeout: raise TimeoutException if the IO doesn't finish in time\n  final withTimeout = IO.sleep(5.seconds).timeout(1.seconds);\n\n  // timeoutTo: return a fallback IO instead of raising\n  final withFallback = IO.sleep(5.seconds).productR(IO.pure(42)).timeoutTo(1.seconds, IO.pure(-1));\n\n  // io-timing\n}\n\nvoid repetitionSnippet() {\n  // io-repetition\n\n  // replicate: run sequentially n times, collect results\n  final rolls = IO.delay(() => Random.secure().nextInt(6) + 1).replicate(3);\n  // IO<IList<int>>\n\n  // replicate_: run n times, discard results\n  final ticks = IO.print('tick').replicate_(5);\n\n  // iterateUntil: keep running until the predicate is satisfied\n  final poll = IO.delay(() => Random.secure().nextInt(100)).iterateUntil((n) => n > 90);\n  // IO<int> \u2014 repeats until a value > 90 is produced\n\n  // io-repetition\n}\n\nvoid traverseSnippet() {\n  // io-traverse\n\n  // traverseIO: apply an IO-valued function to each element, sequentially.\n  // Short-circuits on the first error \u2014 later elements are not evaluated.\n  final validated = ilist([2, 4, 6]).traverseIO(\n    (n) => n.isEven ? IO.pure(n * 10) : IO.raiseError<int>('odd: $n'),\n  ); // IO<IList<int>>([20, 40, 60])\n\n  // traverseIO_: same, but discard the results\n  final logged = ilist(['a', 'b', 'c']).traverseIO_((s) => IO.print('processing: $s'));\n\n  // sequence: collapse IList<IO<A>> into IO<IList<A>>\n  final actions = ilist([IO.pure(1), IO.pure(2), IO.pure(3)]);\n  final sequenced = actions.sequence(); // IO<IList<int>>\n\n  // parTraverseIO / parSequence: same ideas, but run all IOs concurrently\n  final parallel = ilist([1, 2, 3]).parTraverseIO((n) => IO.pure(n * 10));\n\n  // io-traverse\n}\n\nFuture<void> outcomeSnippet() async {\n  // io-outcome\n\n  final outcome = await IO.pure(42).unsafeRunFutureOutcome();\n\n  // fold over the three possible cases: canceled, errored, succeeded\n  outcome.fold(\n    () => print('IO was canceled'),\n    (err, _) => print('IO failed: $err'),\n    (value) => print('IO succeeded: $value'), // IO succeeded: 42\n  );\n\n  // io-outcome\n}\n\nFuture<void> cancelationSnippet() async {\n  // cancelation-1\n  int count = 0;\n\n  // Our IO program\n  final io = IO\n      .pure(42)\n      .delayBy(10.seconds)\n      .onCancel(IO.exec(() => count += 1))\n      .onCancel(IO.exec(() => count += 2))\n      .onCancel(IO.exec(() => count += 3));\n\n  // .start() kicks off the IO execution and gives us a handle to that\n  // execution in the form of an IOFiber\n  final fiber = await io.start().unsafeRunFuture();\n\n  // We immediately cancel the IO\n  fiber.cancel().unsafeRunAndForget();\n\n  // .join() will wait for the fiber to finish\n  // In this case, that's immediate since we've canceled the IO above\n  final outcome = await fiber.join().unsafeRunFuture();\n\n  // Show the Outcome of the IO as well as confirmation that our `onCancel`\n  // handlers have been called since the IO was canceled\n  print('Outcome: $outcome | count: $count'); // Outcome: Canceled | count: 6\n  // cancelation-1\n}\n"}}]);