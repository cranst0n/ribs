import { o as openBlock, c as createElementBlock, a as createStaticVNode, _ as _export_sfc } from "./app.CRf18yaY.js";
const __pageData = JSON.parse('{"title":"","description":"","frontmatter":{"sidebar_position":5},"headers":[],"relativePath":"guide/directives.md","filePath":"guide/directives.md"}');
const _sfc_main = { name: "guide/directives.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<p>Dartdoc supports several <strong>directives</strong> within Dart doc comments. Each directive is then processed during documentation generation, and new text is inserted in place of the directive. Not all directives are supported for package documentation at <a href="https://pub.dev/" target="_blank" rel="noreferrer">https://pub.dev/</a>.</p><p>A doc comment directive consists of either one directive tag, which looks something like <code>{@DIRECTIVE ARG=VALUE ...}</code>, or two such tags (an opening and a closing tag), with content in between.</p><p>The supported directives are listed below:</p><h2 id="nodoc-do-not-include-documentation" tabindex="-1"><code>@nodoc</code> - Do not include documentation <a class="header-anchor" href="#nodoc-do-not-include-documentation" aria-label="Permalink to &quot;`@nodoc` - Do not include documentation&quot;">​</a></h2><p>An element whose doc comment should not appear in the generated documentation can include the <code>@nodoc</code> directive.</p><p>Note that the <code>@nodoc</code> directive does not have curly braces, like most of the other directives.</p><h2 id="category-and-subcategory-categories" tabindex="-1"><code>{@category}</code> and <code>{@subCategory}</code> - Categories <a class="header-anchor" href="#category-and-subcategory-categories" aria-label="Permalink to &quot;`{@category}` and `{@subCategory}` - Categories&quot;">​</a></h2><p>Elements such as libraries and classes can be grouped into categories and sub-categories by adding <code>{@category CATEGORY NAME}</code> and <code>{@subCategory SUB-CATEGORY NAME}</code> in doc comments. Each category then gets its own documentation page, listing all of the categorized elements.</p><h2 id="template-and-macro-templates-and-macros" tabindex="-1"><code>{@template}</code> and <code>{@macro}</code> - Templates and macros <a class="header-anchor" href="#template-and-macro-templates-and-macros" aria-label="Permalink to &quot;`{@template}` and `{@macro}` - Templates and macros&quot;">​</a></h2><p>TODO(srawlins): Document.</p><h2 id="inject-html-injected-html" tabindex="-1"><code>{@inject-html}</code> - Injected HTML <a class="header-anchor" href="#inject-html-injected-html" aria-label="Permalink to &quot;`{@inject-html}` - Injected HTML&quot;">​</a></h2><p>HTML can be rendered unmodified by including it between <code>{@inject-html}</code> and <code>{@end-inject-html}</code> directive tags. The tags take no arguments:</p><div class="language-none vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">none</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/// {@inject-html}</span></span>\n<span class="line"><span>/// &lt;p&gt;Injected HTML.&lt;/p&gt;</span></span>\n<span class="line"><span>/// {@end-inject-html}</span></span></code></pre></div><p>The <code>{@inject-html}</code> directive is only available when the <code>--inject-html</code> flag is passed to <code>dart doc</code>. It is not available for documentation published on <a href="https://pub.dev" target="_blank" rel="noreferrer">https://pub.dev</a>.</p><h2 id="animation-animations" tabindex="-1"><code>{@animation}</code> - Animations <a class="header-anchor" href="#animation-animations" aria-label="Permalink to &quot;`{@animation}` - Animations&quot;">​</a></h2><p>HTML5 videos can be embedded with the <code>{@animation}</code> directive. This directive accepts width and height arguments, and an optional ID argument:</p><div class="language-none vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">none</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/// {@animation 320 240 URL [id=ID]}</span></span></code></pre></div><p>This directive renders the HTML which embeds an HTML5 video.</p><p>The optional ID should be a unique id that is a valid JavaScript identifier, and will be used as the id for the video tag. If no ID is supplied, then a unique identifier (starting with &quot;animation_&quot;) will be generated.</p><p>The width and height must be integers specifying the dimensions of the video file in pixels.</p><h2 id="youtube-youtube-videos" tabindex="-1"><code>{@youtube}</code> - YouTube videos <a class="header-anchor" href="#youtube-youtube-videos" aria-label="Permalink to &quot;`{@youtube}` - YouTube videos&quot;">​</a></h2><p>A YouTube video can be embedded with the <code>{@youtube}</code> directive. This directive accepts width and height arguments, using the following syntax:</p><div class="language-none vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">none</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/// {@youtube 320 240 https://www.youtube.com/watch?v=oHg5SJYRHA0}</span></span></code></pre></div><p>This directive embeds the YouTube video with id &quot;oHg5SJYRHA0&quot; into the documentation page, with a width of 320 pixels, and a height of 240 pixels. The height and width are used to calculate the aspect ratio of the video; the video is always rendered to take up all available horizontal space to accommodate different screen sizes on desktop and mobile.</p><p>The video URL must have the following format: <code>https://www.youtube.com/watch?v=oHg5SJYRHA0</code>. This format can usually be found in the address bar of the browser when viewing a YouTube video.</p><h2 id="tool-external-tools" tabindex="-1"><code>{@tool}</code> - External tools <a class="header-anchor" href="#tool-external-tools" aria-label="Permalink to &quot;`{@tool}` - External tools&quot;">​</a></h2><p>TODO(srawlins): Document.</p><h2 id="canonicalfor-canonicalization" tabindex="-1"><code>{@canonicalFor}</code> - Canonicalization <a class="header-anchor" href="#canonicalfor-canonicalization" aria-label="Permalink to &quot;`{@canonicalFor}` - Canonicalization&quot;">​</a></h2><p>Dartdoc uses some heuristics to decide what the public-facing libraries are, and which public-facing library is the &quot;canonical&quot; location for an element. When that heuristic needs to be overridden, a user can use this directive. Example:</p><div class="language-none vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">none</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/// {@canonicalFor some_library.SomeClass}</span></span></code></pre></div><p>When this directive is used on a library&#39;s doc comment, that library is marked as the canonical library for <code>some_library.SomeClass</code>.</p>', 31)
  ])]);
}
const directives = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  directives as default
};
